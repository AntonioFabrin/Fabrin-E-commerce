# URL do backend (sem barra no final)
# Local:
NEXT_PUBLIC_API_URL=http://localhost:3333
# Produção (Railway) — substitua pela URL real após o deploy:
# NEXT_PUBLIC_API_URL=https://seu-app.up.railway.app
